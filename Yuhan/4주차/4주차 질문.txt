
2장

질문1) <cstring> 파일에 strcpy() 함수의 코드가 들어 있을까 ?
(1) strcpy() 함수의 코드가 들어 있다 ( O , X )	// 실행되는 코드는 아님
답 : X

(2)strcpy() 함수의 원형이 선언되어 있다 ( O , X )
답 : O


질문2) 그러면 strcpy() 함수의 코드는 어디에 있는가 ?
답 : strcpy() 함수의 코드는 컴파일된 바이너리 코드로, 
비주얼스튜디오가 설치된 lib 폴더에 libcmt.lib 파일에 들어 있고
링크 시에 strcpy() 함수의 코드가 exe에 들어간다.

질문3) 그러면 헤더 파일은 왜 사용되는가 ?
답 : 사용자 프로그램에서 strcpy() 함수를 호출하는 구문이 정확한지 
확인하기 위해 컴파일러에 의해 필요

*) cout이나 cin은 모두 <iostream>에 선언된 객체

-----------------------------------------------------------> 

3장

질문1) 생성자는 꼭 있어야하는가 ?
답 : 예. C++ 컴파일러는 객체가 생성될 때, 생성자 반드시 호출

질문 2) 개발자가 클래스에 생성자를 작성해 놓지 않으면 ?
- 컴파일러에 의해 기본 생성자가 자동으로 생성

질문 3) 기본 생성자란 ?
- 클래스에 생성자가 하나도 선언되어 있지 않은 경우,
  컴파일러가 대신 삽입해주는 생성자
- 매개 변수 없는 생성자
- 디폴트 생성자라고도 부름

